# Make Configuration

## Jira Integration

<!-- Change this value to: yes, no, or ask-me -->
jira_mode: no

## How to Change

Edit the `jira_mode` line above:
- `yes` -- always require a Jira issue (you can opt out per-session)
- `no` -- never use Jira
- `ask-me` -- prompt each time (includes "skip Jira" option)
